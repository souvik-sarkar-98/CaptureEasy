# Populated by Maven resource filtering at build time.
# @app.version@ is substituted from pom.xml <properties><app.version>.
app.version=@app.version@
app.name=CaptureEasy
github.repo=souvik-sarkar-98/CaptureEasy
