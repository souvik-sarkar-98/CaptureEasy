<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Properties>
        <Property name="LOG_HOME">${sys:log.home:-${sys:user.home}/.captureeasy/logs}</Property>
        <Property name="PATTERN">[%d{dd MMM yyyy HH:mm:ss.SSS}] %highlight{[%-5level] %class{36} [%L] %M - %msg%xEx%n}{FATAL=red, ERROR=red, WARN=blue, INFO=black, DEBUG=green, TRACE=blue}</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${PATTERN}"/>
        </Console>

        <!-- Updater process log — written by CaptureEasy-Updater-{version}.jar -->
        <RollingFile name="UpdaterFile"
                     fileName="${LOG_HOME}/updater-process-info.html"
                     filePattern="${LOG_HOME}/$${date:yyyy-MM}/updater-process-info-%d{yyyy-MM-dd}.html"
                     immediateFlush="true">
            <HTMLLayout datePattern="dd MMM yyyy HH:mm:ss.SSS" charset="UTF-8"
                        title="CaptureEasy — Updater Process Logs" locationInfo="true"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="trace">
            <AppenderRef ref="Console"     level="info"/>
            <AppenderRef ref="UpdaterFile" level="trace"/>
        </Root>
    </Loggers>

</Configuration>
