package app.techy10souvik.captureeasy.common.events;

public enum EventType {
    APP_LOADED,
    SCREENSHOT_CAPTURED,
    TAKE_SCREENSHOT,
    UPDATE_SCREENSHOT_COUNT, CLOSE_APP, DELETE_SCREENSHOTS, SHOW_LATEST_SCREENSHOT,
    // Add more event types as needed
}
